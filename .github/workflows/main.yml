name: My Workflow

on:
  push:
    branches:
      - main





jobs:
  build:
    runs-on: ubuntu-lastest

    steps:
      - uses: actions/checkout@v3 


      - name: "Login to Docker hub"
        run: docker login --username ${{ secrets.DOCKER_USERNAME }}  --password ${{ secerts.DOCKER_TOKEN }}

